---
updated_at: 2026-01-29T12:57:33.701+05:30
edited_seconds: 610
---
#Crypto 
# The Division Algorithm
- Given any positive integer n and any  non-negative integer a, if we divide a by n, we get an integer quotient q and an integer remainder r that obeys the following relationship:
	- `a=qn+r,    where 0<=r<n; q=[a/n]`
## Quite Important
# Euclidean Algorithm
- Euclid is used to easily find the greatest common division of two integers.
- The greatest common divisor of a and b notation gcd(a,b)
- The greatest common divisor of a and b is the largest integer that divides both a and b.
- We also define gcd(0,0)=0
- The positive integer c is said to be the greatest common divisor of b if 
	- c is a divisor of a and of b
	- Any divisor of a and b is a divisor of c
- `gcd(a,b) = max[k, suck that k|a and k|b]`
- By applying division algorithm, we can state a = q<sub>1</sub>b+ r<sub>1</sub>
- If r<sub>1</sub> - 9, the d = gcd(a,b) = b
## Euclidean Algorithm
- The divison process continues until some zero remainder appears
		a = q<sub>1</sub>b+r<sub>1</sub>
		b = q<sub>2</sub> + r<sub>2</sub>
		r<sub>1</sub> = q<sub>3</sub>r<sub>2</sub>+r<sub>3</sub> 

### Problem:
#### Example:
gcd(1970,1066)